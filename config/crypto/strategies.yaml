schema_version: 1
market: CRYPTO
default_status: DISABLED
strategies:
  ema_adx_trend: {enabled: false}
  donchian_breakout: {enabled: false}
  time_series_momentum: {enabled: false}
  trend_pullback: {enabled: false}
  supertrend_adx_ema: {enabled: false}
  macd_trend_continuation: {enabled: false}
  bollinger_rsi_mean_reversion: {enabled: false}
  vwap_mean_reversion: {enabled: false}
  opening_range_breakout: {enabled: false}
  relative_strength_momentum: {enabled: false}

